@model List<Actor>
@{
    ViewData["Title"] = "Actors";
}

<h1>Actors</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Добавить актера</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Имя</th>
            <th>Дата рождения</th>
            <th>Биография</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var actor in Model)
        {
            <tr>
                <td>@actor.Name</td>
                <td>@actor.BirthDate?.ToString("dd.MM.yyyy")</td>
                <td>@actor.Biography</td>
                <td>
                    <form asp-action="Delete" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="@actor.Id" />
                        <button type="submit" class="btn btn-danger">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    function addActor(actorId) {
        // Реализация добавления актера
        console.log("Добавить актера с ID: " + actorId);
    }
</script>